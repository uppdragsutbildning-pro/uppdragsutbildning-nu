# Uppdragsutbildning.nu – Projektinstruktioner för Claude Code

## Om projektet

**Bolag:** Lumina CTC AB (tidigare/även kallat Credits That Count AB, CTC)
**Produkt:** Uppdragsutbildning.nu – B2B-marknadsplats som kopplar samman organisationer (företag, kommuner, regioner) som söker kompetensutveckling med universitet, högskolor, yrkeshögskolor (YH) och andra utbildningsleverantörer.

Plattformen riktar sig INTE till privatpersoner – alltid B2B med en arbetsgivare/organisation som finansiär.

**Affärsmodell:** Ren success fee – inga listnings- eller abonnemangsavgifter för lärosäten. Uppdragsutbildning.nu tar 20% provision på kursens pris vid bekräftad bokning. Provisionen utlöses vid kursstart, inte vid bokningsbekräftelse.

**Team:**
- Aaron – Product (kommunicerar koncist, korrigerar scope direkt, föredrar avgränsade diagram/visualiseringar)
- Patrik – Onboarding & Sales
- Fredrik – Sales
- Mathieu – Kopplad enbart till ELL (Expert Learning Lab), inte marknadsplatsen

---

## Tech-stack

- **Frontend:** React, TypeScript, Tailwind CSS
- **Backend/databas:** Supabase (auth, databas, storage, Row Level Security, Edge Functions)
- **Hosting:** Vercel (Edge Functions, auto-deploy från `develop`-branch)
- **Betalningar:** Stripe Connect + Stripe Invoicing API
- **AI-lager:** Gemini 2.0 Flash (multimodal, RAG, CPI-analys, SSYK-taggning)
- **Klassificering:** JobTech Taxonomy API (Arbetsförmedlingen, SSYK 2012)
- **UI-verktyg:** Figma Make (lokalt läge, skriver direkt till denna projektmapp)
- **Repo:** `uppdragsutbildning-pro/uppdragsutbildning-nu`

### Supabase-projekt (två separata – blanda ALDRIG ihop dessa)
- **Production:** "Uppdragsutbildning", ID `iswctazjdtirrzswqkor`
- **Staging:** "uppdragsutbildning-staging", ID `eyksngvbrupmxpjzadqp`
- Vercel Preview (develop-branch) ska peka mot staging-projektet för `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`
- Vercel Production (main-branch) ska peka mot production-projektet

---

## Git-flöde (VIKTIGT – följ alltid detta)

1. Jag jobbar i Figma Make, som skriver ändringar direkt till filerna i denna mapp.
2. **Standard:** `git add`, `git commit`, `git push` mot **`develop`**-branchen.
3. Commit-meddelanden ska vara en **kort sammanfattning av vad som faktiskt ändrats** (baserat på diffen), inte generisk text som "update".
4. Innan push till `develop`: visa en kort sammanfattning av ändringarna så jag kan reagera om något ser fel ut.
5. **Pusha eller mergea ALDRIG till `main`** om jag inte uttryckligen ber om det (t.ex. "pusha till produktion" eller "gör detta skarpt").
6. `develop` → Vercel Preview → staging-miljön. `main` → Vercel Production → produktionsmiljön.

### Regler för Supabase-migrationer

- Skapa alltid en migrationsfil (t.ex. `supabase migration new <namn>`) – ändra aldrig databasen direkt utan migration.
- Visa SQL-innehållet för mig innan något körs. Inga "tysta" schemaändringar.
- Bekräfta alltid vilket Supabase-projekt du är kopplad mot (staging `eyksngvbrupmxpjzadqp` eller production `iswctazjdtirrzswqkor`) innan en skrivande operation.
- Kör och verifiera alltid migrationen i **staging** först. Kör mot production endast när jag uttryckligen säger det.
- Destruktiva ändringar (borttagna kolumner, `DROP TABLE`, liknande) kräver alltid explicit godkännande från mig, även i staging.

---

## Kärnfunktioner (MVP – fem stycken)

1. **Quick Scan** – anonym CPI-analys, ingen inloggning krävs
2. **Deep Dive / AI Requirement Builder** – gated bakom kontoskapelse, flaggskeppsdifferentiator
3. SSYK-taggad sökning
4. One-click multi-provider RFP (offertförfrågan till flera leverantörer samtidigt)
5. Jämförelsematris

Post-MVP-tillägg (URL-import, kurskloning, subdomän fas 2–3 m.m.) hålls separat från MVP-scope.

---

## Domänbegrepp

**CPI (Kompetenstrycksindex):** Proprietärt gapanalysverktyg, fyra dimensioner:
- AF – Arbetsförmåga/Arbetsförändring (direkt scoring)
- LF – Leveransfriktion (direkt scoring)
- OK – Omställningskapacitet (omvänd scoring)
- TR – Tillit/Relationer / Transformationsriktning (omvänd scoring)

Scoringformel: Direkt = (svar−1)/4×100. Omvänd = (5−svar)/4×100. Total CPI = snitt av de fyra dimensionerna.
Nivåer: 0–24 Lågt, 25–49 Måttligt, 50–74 Högt, 75–100 Kritiskt.

Två lägen: **Quick Scan** (anonym) och **Deep Dive** (gated bakom konto).

**SSYK 2012:** Arbetsförmedlingens/SCB:s svenska yrkestaxonomi (Yrkesområde → SSYK-grupp → Yrkesroll → Kompetenser), nås via Arbetsförmedlingens publika JobTech Taxonomy API (`taxonomy.api.jobtechdev.se`, ingen API-nyckel krävs). Används för kurstaggning och matchning köpare–leverantör, samt för att koppla Kompetensindex (LI3) mot verkliga yrkesroller och deras kompetenser. Ersatte 2026-09-10 den tidigare ESCO-integrationen (EU:s kompetenstaxonomi).

---

## Nyckelprinciper (styr designbeslut)

- SSYK-taggningskvalitet vid onboarding är plattformens största hävstång för matchningskvalitet – LLM-assisterad taggning ska ha ett mänskligt bekräftelsesteg, inte full automatisering.
- CPI Deep Dive är flaggskeppsdifferentiatorn – prioritera denna vid resurskonflikter.
- Bokningsögonblicket är den mest ekonomiskt kritiska kontaktpunkten (20%-provisionen blir synlig).
- Tystnad i RFP → bokning-flödet är den mest kritiska UX-sårbarheten – kräver systemnivå-eskalering (t.ex. `response_deadline`-fält + cron), inte bara UI-polering.
- Föredra enkla MVP-lösningar anpassade till befintlig stack, inte nya beroenden om det kan undvikas.

---

## Nuläge / pågående arbete

- **Partnermarknadsplatser (spec: [docs/specs/partnermarknadsplatser.md](docs/specs/partnermarknadsplatser.md))** – varumärkta marknadsplatser för bransch-/omställningsorganisationer (kuraterat kursurval från flera leverantörer), som delar infrastruktur med leverantörssubdomänen via en gemensam `marketplaces`-tabell. **2026-09-03: upptäckte vid uppstart att leverantörssubdomän-infrastrukturen (nedan) i själva verket inte fanns i koden alls** trots att den beskrevs som påbörjad – beslutades att bygga båda (`provider_storefront` + `partner_curated`) från grunden i detta uppdrag. **Paket A (datamodell & migrationer) klart** i staging: tabellerna `marketplaces`, `marketplace_branding`, `partner_organizations`, `marketplace_trainings` + RLS, migration `20260903140000_create_marketplaces.sql`, TS-typer i `src/lib/database.types.ts`. **Paket B (UB-admin: marknadsplatshantering) klart**: ny "Marknadsplatser"-flik i `AdminDashboard.tsx` (`src/app/components/admin/MarketplacesTab.tsx`) + tre nya admin-routes (`/admin/marketplaces/new|:id/edit`, `/admin/marketplaces/:id/branding`, `/admin/marketplaces/:id/curation`), ny Storage-bucket `marketplace-branding` (migration `20260903150000_add_marketplace_branding_storage.sql`). **Ej fullständigt verifierat i webbläsare** – ingen admin-lösenord tillgänglig i sessionen, endast routing/build/RLS-mönster verifierat, se avsnittet nedan om produktionsfallback för varför det krävde extra försiktighet. **Paket C (subdomän-routing & theming) klart**: `src/lib/marketplaceContext.tsx` slår upp marknadsplats via subdomän eller `?via=`-fallback, `Root.tsx` visar partnerns logga/tagline/primärfärg i headern och döljer Provider/Admin-länkar. **OBS (2026-09-03): Vercel-projektet saknar helt anpassad domän** (bara `*.vercel.app`) – riktig subdomän-routing (`trr.uppdragsutbildning.nu`) kan inte fungera förrän `uppdragsutbildning.nu` + wildcard-subdomän kopplas i Vercel och DNS, se backlog-punkt nedan. `?via=`-fallbacken fungerar redan och är verifierad. Paket D–H (storefront-query, åtkomstlogik, RFP-attribution, leverantörsportal, deep linking) återstår – observera att Paket C bara temar den delade headern, inte hela sidan (t.ex. knappar/nav-länkar använder ännu inte marknadsplatsens färger via `--marketplace-primary`/`--marketplace-secondary`), det slutförs i takt med att Paket D bygger den faktiska temade storefronten. **Paket D (Storefront-query) klart**: `getMarketplaceTrainingIds()` i `src/lib/marketplaces.ts` scopar `CatalogPage` till en marknadsplats kuraterade urval (`partner_curated`) eller en leverantörs egna publicerade kurser (`provider_storefront`), med eget "inga kurser ännu"-tomtillstånd (beslut #10). Verifierat end-to-end mot staging med både Aarons riktiga TRR-testmarknadsplats (4 kuraterade kurser) och en disponibel `provider_storefront`-testrad. **Paket E (Åtkomstlogik) nedskalat och avslutat 2026-09-03, se backlog-punkt nedan om köparkonton** – "öppen bläddring alltid" var redan sant (ingen auth-spärr fanns någonstans i katalogen sedan tidigare) och `access_mode`-fältet/admin-UI:t fanns redan från Paket B, så inga kodändringar krävdes för det som faktiskt går att bygga idag. **Paket F (RFP-attribution) klart**: nytt `marketplace_id`-fält (nullable, FK → `marketplaces`) på både `custom_requests` och `applications`, migration `20260903160000_add_marketplace_id_attribution.sql`. `RequestFlowPage.tsx` sätter fältet från `useMarketplace()` vid RFP-inskick. Verifierat på schemanivå (FK/join mot staging); **kunde inte klick-testas fullt ut i webbläsaren** eftersom RFP-formuläret redan är trasigt av en annan, sedan tidigare befintlig bugg (se separat spawnad uppgift om `recommended_categories`/`has_provider_match`-kolumnerna som saknas) – bör verifieras i praktiken när den buggen är fixad. `applications`-tabellen fick samma fält för konsistens, men har idag ingen kund-vänd inskicksyta i kodbasen alls (ingen "boka/anmäl"-flow existerar ännu), så inget UI att koppla in där. **Paket G (Leverantörsportal) klart**: ny "Var visas mina kurser"-sida (`src/app/components/provider/ProviderMarketplacesPage.tsx`, route `/provider/marketplaces`) visar per kurs vilka marknadsplatser (öppen/egen butik/partnermarknadsplatser) den syns i; "Via: [namn]"-tagg tillagd i `ProviderRequestsPage.tsx`s RFP-lista och detaljvy. Ny migration `20260903170000_provider_marketplace_visibility.sql` ger leverantörer läsrätt på marknadsplatser som kuraterat in deras kurser (oavsett status). **OBS: denna migration hittade och fixade en självorsakad bugg innan den hann påverka någon annan** – den ursprungliga policyn skapade en cirkulär RLS-referens mot `marketplace_trainings` ("infinite recursion detected in policy"), vilket tillfälligt bröt Paket D:s publika katalog-scopning för alla besökare tills den upptäcktes och åtgärdades (SECURITY DEFINER-funktion istället för direkt subquery) inom samma session. Verifierat i efterhand med JWT-simulering mot Aarons riktiga leverantörskonto (en av hans kurser är faktiskt kuraterad i TRR) – fungerar korrekt. **Paket H (Deep linking & SEO) klart – alla åtta arbetspaket (A–H) i specen nu genomgångna.** `TrainingDetailPage.tsx` sätter `document.title` + `<link rel="canonical">` per kurs (pekar alltid på huvuddomänen `uppdragsutbildning.nu`, aldrig en marknadsplats-subdomän), och skyddar RFP-attribution mot inaktuella marknadsplats-djuplänkar: om kursen inte längre är kuraterad in i marknadsplatsen man kom via skickas RFP:n utan `marketplace_id` istället för att felaktigt attribueras (verifierat i webbläsaren mot både ett kuraterat och ett icke-kuraterat exempel). Grundläggande statiska OG-taggar tillagda i `index.html` (fanns inga alls tidigare, för hela plattformen). **`?via=`-fallbacken (Paket C) är den enda djuplänksmekanism som är fullt verifierad** – riktig subdomän-routing kvarstår overifierad tills Vercel-domänen kopplas (se backlog). **2026-09-03, efter Paket H – Aaron beställde en justering:** en partnermarknadsplats ska ha samma funktionalitet som öppna marknadsplatsen (landningssida med sök, bläddra utbildningar, kompetensindex, efterfråga anpassad utbildning) och **helt vitmärkt** – ingen Uppdragsutbildning.nu-logga/branding ska synas, bara partnerns egen. Ändrat: `Root.tsx` header/footer visar nu bara partnerns logga (eller namn som textfallback) och tagline, ingen samvarumärkning; "För Leverantörer"-länken dold i footer på en marknadsplats; copyright-raden visar partnerns namn istället. Hittade och fixade samtidigt en bugg i `HomePage.tsx` (sökrutan använde `window.location.href` istället för `navigate()`, vilket hade tappat `?via=`-kontexten vid en sidladdning). Alla fyra funktionerna verifierade i webbläsaren under `?via=trr`. **Uppföljning samma dag:** Aaron påpekade att Uppdragsutbildning-loggan ändå syntes kvar – det var webbläsarflikens titel (`document.title`), som bara sattes om på kursdetaljsidan (Paket H) men förblev "Uppdragsutbildning.nu" på alla andra sidor. Fixat i `Root.tsx` (sätts nu site-brett utifrån marknadsplatskontext) + `TrainingDetailPage.tsx` (återställer till marknadsplatsens namn vid avmontering istället för att hårdkoda plattformsnamnet). Verifierat i webbläsaren: fliken visar nu "TRR Karriärstöd" konsekvent över alla sidor i marknadsplatskontext.
- Multi-tenant subdomänarkitektur: varumärkt subdomän per leverantör (t.ex. `chalmers.uppdragsutbildning.nu`), begränsad till logotyp + primärfärg. `source`-flagga + `provider_id`-fält skiljer subdomän-RFP:er (routas till en leverantör) från marknadsplats-RFP:er (distribueras brett). **OBS (2026-09-03): denna rad beskriver ett planerat/förutsatt läge – ingen kod för detta hittades i repot. Byggs nu tillsammans med partnermarknadsplatser ovan, se Paket C/F.**
- CPI-gapanalys är byggd och driftsatt på `develop`. Gemini 2.0 Flash-integration via `api/analyze-cpi.ts`.
- Betalningsarkitektur: Stripe Connect + Invoicing API (`collection_method: 'send_invoice'`, `days_until_due: 30`, `application_fee_amount` för 20%-delningen). Bankgiro/OCR-kompatibilitet är overifierad – behöver bekräftas i sandbox innan lansering.
- Känd historisk bugg (2026-08-05, bör vara fixad): race condition i `src/contexts/AuthContext.tsx` – `onAuthStateChange` saknade `setLoading(true)` innan async `loadProfile()`, vilket orsakade felaktig redirect till `/login`.
- Det kan finnas en gammal nästlad mapp `uppdragsutbildning-nu/` inuti projektroten med dubbletter av `src/`-filer – bekräfta innan du rör den.
- **Kompetensindex-adminvy uppgraderad (2026-09-14)**: `AdminDashboard.tsx`s Kompetensindex-flik visar nu allt en användare fyllt i (kontakt, fritextsvar, SI-poäng, LI-val, SSYK-kompetenser) via en ny detaljvy (`src/app/components/admin/CpiRecordDrawer.tsx`), har fått radera-rad (ny migration `20260914090000_add_cpi_results_delete_policy.sql` – `cpi_results` hade tidigare ingen DELETE-policy alls), sökning + fasetterade filter (bransch/storlek/roll) + paginering (client-side, 20/sida – återanvänder `getPaginationRange()` och `ui/pagination.tsx` som redan fanns men var oanvända), samt CSV-export som respekterar det filtrerade urvalet. **Ej fullständigt klickverifierat i webbläsare** – samma begränsning som Paket B: inget admin-lösenord tillgängligt i sessionen (ett `admin@uppdragsutbildning.nu`-konto finns i staging, men inga inloggningsuppgifter). Verifierat istället via lyckad build och en riktig RLS-simulering mot staging (försökte radera en existerande testrad som anonym roll – blockerades tyst av policyn, raden fanns kvar efteråt), samt att den nya DELETE-policyn använder exakt samma `is_admin()`-funktion som SELECT-policyn redan förlitar sig på i produktion.

---

## Att göra innan lansering

- [ ] **PDF-broschyr-import timar ut** (`api/extract-course-from-pdf.ts`): Gemini multimodal PDF-analys ryms inte inom Vercel Hobby-planens 10s-tak, testat live även efter att timeouten höjts till 9s. Kräver antingen en Vercel Pro-uppgradering (konfigurerbar `maxDuration`, kostar pengar – Aarons beslut) eller en arkitekturändring (t.ex. asynkron bakgrundsbearbetning/polling). Beslut om Pro avvaktas (2026-08-06). Excel-importen (`ProviderCourseExcelImportPage.tsx`) fungerar och kan användas under tiden.
- [x] **Hårdkodad production-fallback i BÅDE `api/create-user.ts` OCH `src/lib/supabase.ts` (fixat 2026-09-17)**: båda föll tidigare tyst tillbaka till hårdkodad production-URL/nyckel om `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` saknades i miljön. `src/lib/supabase.ts` kastar nu ett fel direkt vid saknade env-variabler, `api/create-user.ts` svarar med 500 + tydligt felmeddelande – samma mönster som redan fanns i `api/_lib/supabaseAdmin.ts`. `.env.local` ska ändå alltid ha `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` satta mot staging (`eyksngvbrupmxpjzadqp`) innan du kör dev-servern lokalt – annars kraschar appen direkt istället för att tyst peka fel, vilket är den avsedda skillnaden.
- [ ] **Föräldralös fil `src/app/components/pages/ProviderDashboard.tsx`**: verkar oanvänd/oroutead (skild från den riktiga, routade `provider/ProviderDashboard.tsx`). Bekräfta att den verkligen är död kod och ta i så fall bort den.
- [ ] **AdminDashboards "Alla Utbildningsprogram"-flik kör fortfarande på mockdata** (`AdminDashboard.tsx`, `activeTab === 'trainings'`): samma typ av konvertering som Katalog/Hem/Kursdetaljer redan fick 2026-08-06. Godkänn/Avvisa/Förhandsgranska-knapparna där är dessutom inte kopplade till någon funktion.
- [ ] **ProviderHistoryPage ("Historik") är helt hop-påhittad**: varken statistiken högst upp eller aktivitetsloggen kommer från databasen – hårdkodade exempelvärden. Kräver att en riktig datamodell/query för aktivitetsloggen designas (ingen sådan tabell finns idag) innan sidan kan visa verklig data. "Filtrera"-knappen är av samma anledning inte kopplad.
- [x] **Leverantörsspecifika villkorstexter (fixat 2026-09-17)**: nytt `terms_text`-fält på `providers` (migration `20260917180000_add_provider_terms.sql`), publik sida `/leverantor/{id}/villkor` (`ProviderTermsPage.tsx`) och en ny "Villkor"-flik i Leverantörsportalen (`ProviderTermsSettingsPage.tsx`, route `/provider/villkor`) där leverantören skriver/redigerar sin text. "Anmälan bekräftad"-mejlet (`applicationConfirmed` i `emailTemplates.ts`) länkar nu till villkoren när de finns. Skrivrätt går via en ny SECURITY DEFINER-funktion `update_provider_terms(target_provider_id, new_terms_text)` snarare än en bred UPDATE-policy på `providers` – leverantörer har annars ingen egen UPDATE-rättighet alls på tabellen (bara admins), och en bred policy hade oavsiktligt exponerat fält som `name`/`is_active` för redigering. **Säkerhetsbugg hittad och fixad innan commit**: första versionen av funktionen använde `IF NOT (is_admin() OR target_provider_id = get_user_provider_id())`, men för en oautentiserad anropare är `get_user_provider_id()` NULL, vilket gör hela villkoret NULL istället för `true`/`false` – i PL/pgSQL körs då inte `RAISE EXCEPTION`, så vem som helst hade kunnat skriva över vilken leverantörs villkor som helst. Fixat genom att explicit kolla `caller_provider_id IS NOT NULL` innan jämförelsen, verifierat i staging (anonymt anrop ger nu `P0001: Not authorized...`, raden opåverkad). Testat end-to-end i webbläsaren mot staging (tom- och ifyllt-tillstånd, radbrytningar bevaras).
- [ ] **E-postnotifieringar (v1) – driftsatt i Production 2026-08-07, fullständig verifiering kvarstår**: `main` synkad med `develop` (b3f5a33), migrationerna (inkl. den tidigare ej migrerade `add_response_deadline`) körda mot production-Supabase, vault-secrets och samtliga Vercel-miljövariabler (`BREVO_API_KEY`, `ADMIN_NOTIFICATION_EMAIL`, `NOTIFY_WEBHOOK_SECRET`, `CRON_SECRET`, `APP_URL`) satta med egna production-värden. Ett begränsat test (allmän RFP utan kopplad kurs) återstår/pågår – se punkten nedan om att production saknar kursdata för varför resten av flödet inte kan fullständigt verifieras än.
- [ ] **Production saknar allt kursinnehåll** (identifierat 2026-08-07 vid produktionslansering av e-postnotifieringarna): `providers`, `trainings`, `categories`, `scheduled_starts` och `applications` har alla 0 rader i production. Katalogen/kurssidorna är alltså helt tomma för riktiga besökare, och notifieringsmejl som kräver en kopplad kurs (Ny offertförfrågan, Ny kursanmälan, leverantörshalvan av Bokning bekräftad, Anmälan bekräftad) kan varken triggas av riktiga användare eller testas förrän minst en leverantör/kurs finns på plats. Kräver en riktig onboarding-process för de första leverantörerna (eller en medveten seed) innan lanseringen är meningsfull för användare.
- [ ] **Mall-redigering via admin-UI** (medvetet uteslutet ur v1, 2026-08-07): e-postmallarna är hårdkodade i `api/_lib/emailTemplates.ts`. Om ni vill kunna redigera texten utan kodändring/deploy krävs att mallarna flyttas till databasen plus ett redigeringsgränssnitt i adminpanelen.
- [ ] **Ingen kundstatussida för offertförfrågningar** (identifierat 2026-08-07 när länkar lades till i notifieringsmejlen): mejl 2 ("Leverantören har svarat") och 4 ("Förfrågan avböjd") går till kunden som skickade en offertförfrågan, men det finns ingen inloggning eller "mina förfrågningar"-vy för den kunden att länka till – de två mejlen saknar därför en call-to-action-länk (till skillnad från övriga fem). Kräver en kundinloggning/statussida innan länkar kan läggas till där också.
- [ ] **Justera brandingen i notifieringsmejlen** (identifierat 2026-08-07): en första omgång gjordes redan (avsändarnamn och logga-text i `api/_lib/emailTemplates.ts`/`sendEmail.ts` ändrat från "Uppdragsutbildning.nu" till "Uppdragsutbildning"), men en mer genomgripande branding-pass återstår – t.ex. riktig logotypbild istället för textlogga, och en bredare koll av färger/typografi i mejlens HTML mot varumärkesriktlinjerna.
- [x] **Leverantörers `contact_email` saknas i staging – tre av sju notifieringsmejl uteblir tyst (fixat 2026-09-17)**: `notify-webhook.ts` skickade bara mejl om `providers.contact_email` var ifyllt, annars hoppades det över helt utan att ens en `email_log`-rad skapades. `sendEmail()` (`api/_lib/sendEmail.ts`) accepterar nu `to: string | null` och skriver alltid en `email_log`-rad – med `status='failed'` och en tydlig felorsak ("Mottagarens e-postadress saknas...") när mottagaren saknas, istället för att hoppa över tyst. `notify-webhook.ts`s tre påverkade call-sites ("Ny offertförfrågan", "Ny kursanmälan", leverantörshalvan av "Bokning bekräftad") anropar nu `sendEmail` ovillkorligen. Krävde migration `20260917100000_allow_null_email_log_recipient.sql` (`recipient_email` var `NOT NULL`, men en misslyckad-pga-saknad-adress-rad har ingen adress att logga). Admin-formuläret för att skapa leverantör (`AdminDashboard.tsx`) kräver nu `contact_email` (både `required` i UI och en explicit check i `handleCreateProvider()`) för nya leverantörer. **Kvarstår, se punkter nedan**: (1) ingen admin-vy visar `email_log` alls ännu, så de nya `failed`-raderna är bara synliga via SQL/Supabase-dashboarden, inte i UI:t; (2) `providers.contact_email` är fortfarande nullable på databasnivå – en hård `NOT NULL`-constraint gick inte att lägga till nu eftersom samtliga befintliga leverantörer i staging saknar värdet (skulle kräva en backfill av riktiga kontaktadresser först, ett affärsbeslut snarare än en kodändring); (3) ingen leverantörs-självbetjäningssida finns för att komplettera `contact_email` på en redan skapad leverantör – enda skapandevägen är admin-formuläret.
- [ ] **Ingen admin-vy för `email_log`** (identifierat 2026-09-17 vid fix av tyst-uteblivna leverantörsmejl ovan): tabellen har funnits sedan e-postnotifieringarna (2026-08-07) men har aldrig haft ett UI – varken lyckade eller misslyckade utskick går att se utan att fråga databasen direkt. Nu när `sendEmail()` konsekvent loggar `status='failed'` med felorsak (t.ex. saknad `contact_email`) blir avsaknaden av en vy mer akut. Kräver en enkel läsande admin-flik (lista + filter på status/typ, liknande mönstret i Kompetensindex-fliken).
- [ ] **`providers.contact_email` saknar `NOT NULL`-constraint på databasnivå** (identifierat 2026-09-17): UI:t kräver nu fältet vid skapande av ny leverantör, men schemat tillåter fortfarande `NULL`, och samtliga befintliga leverantörer i staging har det ännu ej ifyllt. Kräver en backfill av riktiga kontaktadresser för befintliga leverantörer (Aarons/Patriks jobb, inte en kodändring) innan en `ALTER COLUMN ... SET NOT NULL`-migration kan köras utan att gå sönder.
- [ ] **AdminDashboards "Alla Förfrågningar"-flik saknar helt funktion för att ändra status** (identifierat 2026-08-07 vid produktionstest av e-postnotifieringarna): kortlistan (`AdminDashboard.tsx`, `activeTab === 'leads'`) är rent läsande – statusbadgen är bara ett textspann, inget klick, ingen modal, ingen knapp för att svara/acceptera/avböja en förfrågan. Statusändringar görs idag bara via andra vyer (leverantörens `/provider/requests`, eller kundens anmälningsflöde), vilket gör att en admin inte kan hantera en obesvarad/omatchad förfrågan direkt från sin egen översikt. Bör byggas som en riktig funktion (t.ex. klick öppnar detaljvy med svara/acceptera/avböj-knappar).
- [ ] **`partner_organization_members`-platshållartabell (fas 2, partnermarknadsplatser) medvetet inte skapad i Paket A** (2026-09-03): specen ([docs/specs/partnermarknadsplatser.md](docs/specs/partnermarknadsplatser.md) avsnitt 5) föreslår en tom/förberedd tabell för verifierat medlemskap (kopplat per `buyer_organization_id`, inte individ) för att slippa en extra migrering i fas 2. Bedömdes som YAGNI i Paket A – läggs till som egen migration när fas 2-arbetet (delegerad partner-självbetjäning, verifierat medlemskap) faktiskt påbörjas.
- [ ] **Vercel saknar anpassad domän – blockerar riktig subdomän-routing för marknadsplatser** (identifierat 2026-09-03 i Paket C): projektet (`prj_1GL0IE9NQoTb2QXJ2Ih4P8xIo3tv`) har idag bara `*.vercel.app`-domäner, ingen `uppdragsutbildning.nu`. Kräver att Aaron (kontoåtkomst till Vercel + DNS-leverantör) lägger till `uppdragsutbildning.nu` som anpassad domän i Vercel samt en wildcard-subdomän (`*.uppdragsutbildning.nu`) och matchande DNS-poster. `src/lib/marketplaceContext.tsx` har redan stöd för hostname-baserad routing och kräver ingen kodändring när domänen väl är kopplad – `?via=`-fallbacken fungerar oberoende av detta redan nu.
- [x] **Paket C temar nu hela sidan, inte bara headern (fixat 2026-09-17)**: `Root.tsx`, `CatalogPage`, `HomePage`, `TrainingDetailPage`, `KompetensindexPage`, `RequestFlowPage`, `OmTjansten` och `CourseStartCard` använder nu `var(--marketplace-primary)`/`var(--marketplace-secondary)` för knappar, nav-länkar, badges och hero-sektioner istället för hårdkodade `blue-600`/`green-600`. Nya standardvärden för variablerna i `src/styles/theme.css` matchar dagens blue-600/green-600, så plattformen ser oförändrad ut utan marknadsplatskontext. Semantiska status-/innehållsfärger (`StatusBadge.tsx`, CPI-dimensionsfärger, kursformat-badges) är medvetet oförändrade – de kodar innehåll/status, inte varumärke. **Kvarstår**: `HomePage.tsx`s två handbyggda hero-kort ("Sök utbildning"/"Kompetensindex") använder hårdkodade hex-värden direkt i `style={{}}` (t.ex. `#1D4ED8`, `#16A34A`), inte Tailwind-klasser – en pixelprecis design med skuggor/kantlinjer som medvetet lämnades orörd (större risk att rubba designen, egen uppföljning om helt vitmärkta hero-kort önskas). Verifierat i webbläsaren mot staging: öppen katalog/kursdetalj oförändrad, TRR-marknadsplatsen (`?via=trr`) lila genomgående (hero, nav, filter, badges, kursdetaljsidans CTA-kort).
- [x] **`CatalogPage`s kategori-/leverantörsfilter scopade till marknadsplatsen (fixat 2026-09-17)**: filtren visade tidigare alla kategorier/leverantörer i hela öppna katalogen även när man bläddrade en kuraterad marknadsplats. Härleds nu från vilka kategorier/leverantörer som faktiskt förekommer bland `getMarketplaceTrainingIds()`s scopade tränings-id:n (ny state `scopedCategoryIds`/`scopedProviderIds` i `CatalogPage.tsx`, `null` = ingen begränsning för öppna katalogen). Verifierat i webbläsaren mot staging: öppen katalog visar alla 8 kategorier/6 leverantörer, TRR-marknadsplatsen (`?via=trr`) visar bara de 3/4 som faktiskt finns i dess kuraterade urval, och filterinteraktionen fungerar som tidigare.
- [ ] **`HomePage` är medvetet inte scopad till marknadsplatskontext i Paket D** (2026-09-03): specens avsnitt 7.2 pekar ut kursbläddringen (`CatalogPage`) som den yta som ska återanvändas/scopas – `HomePage` gör inga egna `trainings`-queries idag (statiskt/mock-innehåll) och lämnades därför orörd. Om en riktig "storefront-landningssida" (skärm 5 i specens avsnitt 12) med marknadsplats-specifikt hero-innehåll önskas är det ett separat designarbete, inte del av denna arbetspaketindelning.
- [ ] **Dynamiska OG-metataggar per marknadsplats fungerar inte i en ren React SPA** (2026-09-03, Paket H): länkförhandsgranskningar (Facebook/LinkedIn/Slack m.fl.) exekverar inte JS, så `document.title`/`<link rel="canonical">` som sätts client-side (som nu görs i `TrainingDetailPage.tsx`) syns för Googlebot men inte för sociala plattformars förhandsgranskningar. En riktig lösning kräver en Vercel Edge Function/middleware som känner igen crawler-user-agents och serverar förrenderade taggar baserat på host+path. Statiska (icke-dynamiska) OG-taggar för hela plattformen finns nu i `index.html` som grundskydd.
- [x] **Kanonisk kurs-URL är nu slug-baserad, `/kurs/{slug}` (fixat 2026-09-17)**: ny kolumn `trainings.slug` (migration `20260917153000_add_slug_to_trainings.sql`, `NOT NULL UNIQUE`, backfyllad från titel för befintliga rader med löpnummer-suffix vid krock). Ny route `/kurs/:slug` (`src/app/routes.tsx`) renderar samma `TrainingDetailPage`; den gamla `/training/:id` (UUID) **fortsätter fungera som alias** (medvetet val – redan delade länkar, Google-indexering och tidigare skickade e-postmejl ska inte gå sönder), men `<link rel="canonical">` pekar nu alltid på `/kurs/{slug}` oavsett vilken route besökaren kom in via. Slugen genereras automatiskt från titeln (`src/lib/slug.ts`, `slugify()` + `generateUniqueTrainingSlug()`) **en gång vid skapande** – ändras aldrig vid senare redigering, så en indexerad länk inte bryts om titeln justeras. Inget redigerbart slug-fält i UI:t (medvetet val, se nedan). Kopplat in i samtliga skapandevägar: leverantörens kursformulär (`ProviderCourseFormPage.tsx`), "duplicera kurs" (`ProviderCoursesPage.tsx`, som även fick en bugg fixad – dupliceringen kopierade tidigare alla kolumner rakt av inklusive `id`/`created_at`/`updated_at`/`views`/`leads`, vilket nu även uteslutit `slug` uttryckligen så en kopia inte kan ärva originalets slug), och Excel-importen (`CourseImportPreview.tsx`/`courseImport.ts`, en rad i taget sekventiellt så att slug-krockar inom samma importfil upptäcks mot databasen). Interna länkar (`CatalogPage.tsx`, `ProviderCoursesPage.tsx`s förhandsgranskning, e-postmallens "Anmälan bekräftad"-länk i `notify-webhook.ts`) pekar nu på `/kurs/{slug}`. **Medvetet inte ändrat**: `HomePage.tsx`s länkar till `/training/{id}` rör sig om statisk mockdata (redan ett känt, separat problem, se övrig backlogg) och lämnades orörda; den föräldralösa/oroutade `src/app/components/pages/ProviderDashboard.tsx` (se egen backlogg-punkt om död kod) rördes inte heller.
- [ ] **Vercel-funktioner körs i USA (`iad1`), inte EU – GDPR-fråga upptäckt 2026-09-16**: Supabase (både production `iswctazjdtirrzswqkor` och staging `eyksngvbrupmxpjzadqp`) ligger i `eu-west-1` (Irland) för databas/auth/storage, men Vercel-teamet ("Uppdragsutbildning's projects") kör på **Hobby-planen** och har ingen `regions`-nyckel i `vercel.json` – verifierat direkt mot senaste production-deploy (`dpl_sanULRn1GzVv2u2VSJGvpxwnVwZ6`): `"regions": ["iad1"]` (Washington D.C., USA). Alla `/api/*`-funktioner som hanterar personuppgifter i flykt (CPI-analys, RFP-inskick, e-postnotifieringar m.fl.) körs alltså på amerikansk infrastruktur innan datan når den EU-baserade databasen. Regionsval för Vercel-funktioner (`regions` i `vercel.json`) kräver **Pro-plan eller högre** – går inte att låsa på Hobby ens om nyckeln läggs till. Kräver: (1) beslut om Vercel Pro-uppgradering (kostar pengar – Aarons beslut, jfr PDF-broschyr-timeout-punkten ovan som väntar på samma typ av beslut), (2) om Pro köps, sätt `"regions": ["fra1"]` eller `["dub1"]` i `vercel.json` och verifiera i staging först, (3) bekräfta att detta täcks av DPA/SCCs med Vercel oavsett utfall.
- [ ] **`access_mode = 'gated'` går inte att faktiskt verkställa – inget köparkontosystem finns i hela plattformen** (identifierat 2026-09-03 vid planering av Paket E): specens beslut #4/#5 förutsätter att en besökare kan ha eller skapa ett "köparkonto" för att låsa RFP/bokning bakom inloggning, men `profiles.role` tillåter idag bara `admin`/`provider` – ingen köpar-/kundroll, ingen registrerings- eller inloggningssida, och RLS på `custom_requests`/`applications` tillåter uttryckligen anonym `INSERT` (`TO PUBLIC WITH CHECK (true)`, `supabase-migrations/02_row_level_security.sql`). Detta är inte specifikt för partnermarknadsplatser – det påverkar hela plattformens RFP/bokningsflöde och är därför ett eget, större initiativ (ny roll, registrering/inloggning, sessionshantering, uppdaterad RLS) snarare än en deluppgift i denna spec. Beslutat 2026-09-03: bygg inte en låtsas-inloggningsspärr utan en riktig destination – `access_mode`-fältet och admin-UI:t (Paket B) finns redan och sparas korrekt, men `gated`/`mixed` har idag samma faktiska beteende som `open` (öppen bläddring, ingen spärr på RFP/bokning) tills köparkonton finns på plats.

---

## UI-riktlinjer

Minimalistisk/luftig estetik (Notion/Linear-stil): typsnitt Inter, blue-600 som primärfärg, green-600 som CTA-färg, vita kort. Undvik mörka overlays och tungt färgade sektioner.

---

## Arbetssätt

- Skriv på svenska om inte annat anges.
- Håll ändringar avgränsade till MVP-scope (avsnitt "Kärnfunktioner") – flagga om en uppgift egentligen är post-MVP.
- Kom ihåg tekniska och affärsmässiga beslut vi fattar under sessionen.
- Bekräfta att du förstått git-flödet ovan innan du kör din första commit i en ny session.
